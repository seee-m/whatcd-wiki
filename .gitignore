node_modules/
dist/
data/*.sqlite
data/*.sqlite-*
.env
.DS_Store

# Source data dump (~3.4GB) -- lives here for the import scripts to read,
# but is source material, not project code. The built whatcd.sqlite it
# produces lives outside this repo entirely (~/Library/Application
# Support/whatcd-wiki), so it's never staged either.
whatcd-goodbye/

# Claude Code local (per-machine) settings, not shared project config.
.claude/settings.local.json

# Local-only maintenance script (scales the prod machine up/down via
# flyctl and runs the Discogs bulk import against it) -- run by hand from
# a terminal that's already `fly auth login`'d, not part of the app --
# plus its usage instructions, kept alongside it.
api/import/update-discogs-videos-prod.sh
api/import/update-discogs-videos-prod.txt

# Personal workflow notes for Claude Code sessions on this machine, not
# shared project documentation.
CLAUDE.md
